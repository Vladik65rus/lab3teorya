# Лабораторная работа №3: Разработка синтаксического анализатора (парсера)

## 📋 Содержание
- [О проекте](#о-проекте)
- [Сведения об авторе](#сведения-об-авторе)
- [Постановка задачи](#постановка-задачи)
- [Вариант задания](#вариант-задания)
- [Разработка грамматики](#разработка-грамматики)
- [Классификация грамматики по Хомскому](#классификация-грамматики-по-хомскому)
- [Метод анализа](#метод-анализа)
- [Диагностика и нейтрализация синтаксических ошибок](#диагностика-и-нейтрализация-синтаксических-ошибок)
- [Тестовые примеры](#тестовые-примеры)
- [Реализация](#реализация)
- [Интерфейс программы](#интерфейс-программы)
- [Технологии](#технологии)
- [Установка и запуск](#установка-и-запуск)

## 🎯 О проекте

**Цель работы:** Изучить назначение и принципы работы синтаксического анализатора в структуре компилятора. Спроектировать грамматику, построить соответствующую схему метода анализа грамматики и выполнить программную реализацию парсера с нейтрализацией синтаксических ошибок методом Айронса. Интегрировать разработанный модуль в ранее созданный графический интерфейс языкового процессора.

## 👤 Сведения об авторе

| Параметр | Значение |
|----------|----------|
| **Студент** | Савченко Владислав |
| **Группа** | АВТ-313 |
| **Вариант** | 75 (Rust) |
| **Язык реализации** | Python 3 |
| **Год** | 2026 |

## 📝 Постановка задачи

Разработать синтаксический анализатор (парсер) в соответствии с индивидуальным вариантом задания, интегрировать его в приложение из лабораторной работы №1 и обеспечить наглядный вывод результатов анализа.

### Требования к разработке парсера:

1. **Разработать грамматику** для заданной синтаксической конструкции
2. **Построить схему метода анализа** на основе разработанной грамматики
3. **Выполнить программную реализацию** алгоритма работы синтаксического анализа
4. **Реализовать алгоритм нейтрализации** синтаксических ошибок методом Айронса
5. **Входные данные** — строка (текст программного кода) из области редактирования
6. **Выходные данные:**
   - При успешном анализе корректной строки — сообщение об отсутствии ошибок
   - При обнаружении ошибок — таблица с описанием каждой ошибки

### Требования к интеграции и интерфейсу:

- Встроить парсер в ранее разработанный интерфейс и связать его с кнопкой «Синтаксический анализ»
- Окно вывода результатов должно содержать таблицу ошибок со столбцами:
  - Неверный фрагмент (символ или фрагмент, вызвавший ошибку)
  - Местоположение (номер строки, позиция символа)
  - Описание ошибки
- Отображать общее количество найденных ошибок
- Реализовать навигацию по ошибкам: при щелчке на строке таблицы курсор в области редактирования устанавливается на позицию ошибочного фрагмента

## ⚙️ Вариант задания

### Номер варианта: 75

### Язык: Rust (упрощенный синтаксис функций)

### Описание синтаксической конструкции:

**Объявление функции в языке Rust:**

```rust
fn имя_функции ( параметры ) -> тип_возврата {
    return выражение ;
}
```

**Компоненты:**
- `fn` — ключевое слово объявления функции
- `имя_функции` — идентификатор
- `параметры` — список вида `имя: тип`, разделенный запятыми
- `тип_возврата` — `i32`, `i64`, `f32`, `f64`, `bool`
- `выражение` — арифметическое выражение с операциями `+`, `-`, `*`, `/` и скобками
- `;` — обязательный признак конца оператора `return`

**Примеры корректных входных строк:**

1. `fn calc(a: i32, b: i32) -> i32 { return a + b } ;`
2. `fn empty() -> bool { return true } ;`
3. `fn complex(x: f64) -> f64 { return (x * 2) + 5 } ;`

**Перечень допустимых лексем:**

| Тип лексемы | Значения | Примеры |
|-------------|----------|---------|
| Ключевое слово | `fn`, `return` | `fn`, `return` |
| Тип данных | `i32`, `i64`, `f32`, `f64`, `bool` | `i32`, `bool` |
| Идентификатор | буквы, цифры, `_`, начинается с буквы | `calc`, `x`, `sum1` |
| Число | последовательность цифр | `42`, `0`, `100` |
| Операторы | `=`, `+`, `-`, `*`, `/`, `->` | `+`, `-`, `->` |
| Разделители | `(`, `)`, `{`, `}`, `;`, `:`, `,`, пробел | `(`, `)`, `;` |

## Разработка грамматики

### Полное определение грамматики в нотации БНФ:

### Грамматика в компактной форме (используется в реализации):

Где:
- `S` — начальный нетерминал (объявление функции)
- `P` — параметры функции
- `T` — тип данных
- `E` — арифметическое выражение
- `id` — идентификатор (терминал)
- `num` — число (терминал)

## Классификация грамматики по Хомскому

Данная грамматика относится к **контекстно-свободной грамматике (КС-грамматике, тип 2)** по классификации Хомского.

### Обоснование:

1. Все правила имеют вид `A → α`, где `A` — нетерминал, `α` — строка из терминалов и нетерминалов
2. Левая часть каждого правила состоит ровно из одного нетерминала
3. Правила не зависят от контекста (окружения)
4. Грамматика описывает вложенные структуры (например, выражения со скобками)

### Примеры правил, доказывающих КС-грамматику:

| Правило | Обоснование |
|---------|-------------|
| `E → E + E` | Леворекурсивное правило, допустимое для КС-грамматик |
| `E → ( E )` | Вложенная структура, невозможная в регулярных грамматиках |
| `P → id : T , P` | Рекурсивное правило для списка параметров |
| `S → fn id ( P ) -> T { return E } ;` | Сложная структура с несколькими компонентами |

Грамматика **не является регулярной (тип 3)** из-за наличия рекурсивных правил и вложенных конструкций. В регулярных грамматиках все правила должны иметь вид `A → aB` или `A → a`, что не выполняется для правил вида `E → ( E )` и `E → E + E`.

## Метод анализа

### Выбранный метод: Рекурсивный спуск (Recursive Descent)

Метод рекурсивного спуска является реализацией нисходящего синтаксического анализа, где для каждого нетерминала грамматики создается своя функция, вызывающая другие функции для разбора вложенных конструкций.

### Алгоритм работы парсера (псевдокод):

```python
def parse_S():
    """S -> fn id ( P ) -> T { return E } ;"""
    
    # Проверка ключевого слова fn
    if not match('KEYWORD', 'fn'):
        error("Ожидалось 'fn'")
        synchronize()  # Метод Айронса
        return False
    
    # Проверка идентификатора
    if not match('IDENTIFIER'):
        error("Ожидался идентификатор")
        synchronize()
    
    # Проверка открывающей скобки
    if not match('LPAREN'):
        error("Ожидалась '('")
    
    # Разбор параметров (если есть)
    if current_token.type != 'RPAREN':
        parse_P()
    
    # Проверка закрывающей скобки
    if not match('RPAREN'):
        error("Ожидалась ')'")
    
    # Проверка стрелки ->
    if not match('MINUS') or not match('ARROW'):
        error("Ожидалось '->'")
    
    # Разбор типа возврата
    parse_T()
    
    # Проверка открывающей фигурной скобки
    if not match('LBRACE'):
        error("Ожидалась '{'")
    
    # Проверка ключевого слова return
    if not match('KEYWORD', 'return'):
        error("Ожидалось 'return'")
    
    # Разбор выражения
    parse_E()
    
    # Проверка закрывающей фигурной скобки
    if not match('RBRACE'):
        error("Ожидалась '}'")
    
    # Проверка точки с запятой
    if not match('SEMICOLON'):
        error("Ожидалась ';'")
    
    return True


def parse_E():
    """E -> id | num | ( E ) | E + E | E - E | E * E | E / E"""
    
    token = current_token()
    
    if token.type in ['IDENTIFIER', 'NUMBER']:
        next_token()  # Потребляем id или число
    
    elif token.type == 'LPAREN':
        next_token()  # Потребляем '('
        parse_E()      # Разбор выражения внутри скобок
        if not match('RPAREN'):
            error("Ожидалась ')'")
    
    else:
        error("Ожидалось выражение (идентификатор, число или скобки)")
        return
    
    # Обработка бинарных операторов
    if current_token().type in ['PLUS', 'MINUS', 'MULTIPLY', 'DIVIDE']:
        next_token()  # Потребляем оператор
        parse_E()      # Разбор правого операнда4
```
def _add_error(self, expected, found):
    """Добавляет ошибку в список (нейтрализация - анализ продолжается)"""
    token = self._current_token()
    if token:
        self.errors.append({
            'fragment': token['value'],
            'line': token['line'],
            'pos': token['start'],
            'message': f'Ожидалось "{expected}", найдено "{token["value"]}"'
        })
    else:
        self.errors.append({
            'fragment': 'конец строки',
            'line': 1,
            'pos': 1,
            'message': f'Ожидалось "{expected}", достигнут конец ввода'
        })
    # Анализ ПРОДОЛЖАЕТСЯ - ключевая особенность метода Айронса


def synchronize(self, follow_set):
    """Синхронизация: пропуск токенов до встречи с токеном из follow_set"""
    while self._current_token() and self._current_token().type not in follow_set:
        self._add_error("синхронизация", self._current_token().value)
        self.pos += 1

## Скриншот интерфейса:
### Пример 1: Корректная строка
<img width="1919" height="1028" alt="image" src="https://github.com/user-attachments/assets/2f5fb927-2fc4-4526-8f4a-b46fdbe48364" />
### Пример 2: Пропущена запятая между параметрами

<img width="1919" height="1030" alt="image" src="https://github.com/user-attachments/assets/cc06eaf2-0626-481f-bf47-4f6f8e5c05db" />

### Пример 3: Пропущена точка с запятой после закрывающей скобки

<img width="1916" height="1031" alt="image" src="https://github.com/user-attachments/assets/b71e73c1-8662-4ca3-b7a4-c417072fe1f5" />

### Пример 4: Неверный тип возврата

<img width="1914" height="1033" alt="image" src="https://github.com/user-attachments/assets/82b215b6-7a39-4a71-8d44-17a12d399d2b" />

### Пример 5: Несколько ошибок в одной строке (демонстрация метода Айронса)****
<img width="1919" height="1028" alt="image" src="https://github.com/user-attachments/assets/fed5a89f-6266-44af-b38f-66973e6b4c59" />

###Пример 6: Многострочный пример
<img width="1919" height="1017" alt="image" src="https://github.com/user-attachments/assets/b02f6291-14ed-49fa-a1dc-eda0df8bb9bb" />

