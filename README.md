# Лабораторная работа №3: Разработка синтаксического анализатора (парсера)

## 📋 Содержание
- [О проекте](#о-проекте)
- [Сведения об авторе](#сведения-об-авторе)
- [Постановка задачи](#постановка-задачи)
- [Вариант задания](#вариант-задания)
- [Разработка грамматики](#разработка-грамматики)
- [Классификация грамматики по Хомскому](#классификация-грамматики-по-хомскому)
- [Метод анализа](#метод-анализа)
- [Диагностика и нейтрализация синтаксических ошибок](#диагностика-и-нейтрализация-синтаксических-ошибок)
- [Тестовые примеры](#тестовые-примеры)
- [Реализация](#реализация)
- [Интерфейс программы](#интерфейс-программы)
- [Технологии](#технологии)
- [Установка и запуск](#установка-и-запуск)

## 🎯 О проекте

**Цель работы:** Изучить назначение и принципы работы синтаксического анализатора в структуре компилятора. Спроектировать грамматику, построить соответствующую схему метода анализа грамматики и выполнить программную реализацию парсера с нейтрализацией синтаксических ошибок методом Айронса. Интегрировать разработанный модуль в ранее созданный графический интерфейс языкового процессора.

## 👤 Сведения об авторе

| Параметр | Значение |
|----------|----------|
| **Студент** | Савченко Владислав |
| **Группа** | АВТ-313 |
| **Вариант** | 75 (Rust) |
| **Язык реализации** | Python 3 |
| **Год** | 2026 |

## 📝 Постановка задачи

Разработать синтаксический анализатор (парсер) в соответствии с индивидуальным вариантом задания, интегрировать его в приложение из лабораторной работы №1 и обеспечить наглядный вывод результатов анализа.

### Требования к разработке парсера:

1. **Разработать грамматику** для заданной синтаксической конструкции
2. **Построить схему метода анализа** на основе разработанной грамматики
3. **Выполнить программную реализацию** алгоритма работы синтаксического анализа
4. **Реализовать алгоритм нейтрализации** синтаксических ошибок методом Айронса
5. **Входные данные** — строка (текст программного кода) из области редактирования
6. **Выходные данные:**
   - При успешном анализе корректной строки — сообщение об отсутствии ошибок
   - При обнаружении ошибок — таблица с описанием каждой ошибки

### Требования к интеграции и интерфейсу:

- Встроить парсер в ранее разработанный интерфейс и связать его с кнопкой «Синтаксический анализ»
- Окно вывода результатов должно содержать таблицу ошибок со столбцами:
  - Неверный фрагмент (символ или фрагмент, вызвавший ошибку)
  - Местоположение (номер строки, позиция символа)
  - Описание ошибки
- Отображать общее количество найденных ошибок
- Реализовать навигацию по ошибкам: при щелчке на строке таблицы курсор в области редактирования устанавливается на позицию ошибочного фрагмента

## ⚙️ Вариант задания

### Номер варианта: 75

### Язык: Rust (упрощенный синтаксис функций)

### Описание синтаксической конструкции:

**Объявление функции в языке Rust:**

```rust
fn имя_функции ( параметры ) -> тип_возврата {
    return выражение ;
}
```
## 📐 Разработка грамматики
### Грамматика в нотации БНФ (Backus-Naur Form):
<программа>      ::= <объявление_функции>
<объявление_функции> ::= fn <идентификатор> ( <параметры> ) -> <тип> { return <выражение> } ;
<параметры>      ::= <параметр> { , <параметр> } | ε
<параметр>       ::= <идентификатор> : <тип>
<тип>            ::= i32 | i64 | f32 | f64 | bool
<выражение>      ::= <терм> { (+ | -) <терм> }
<терм>           ::= <фактор> { (* | /) <фактор> }
<фактор>         ::= <число> | <идентификатор> | ( <выражение> )
<идентификатор>  ::= буква { буква | цифра | _ }
<число>          ::= цифра { цифра }
